# 开发 Changelog（Justyn）

基于 OtterWiki 上游定制开发的提交记录，时间跨度 **2026-08-27 ~ 2026-09-08**，共 44 个提交。
不含上游社区（Ralph Thesen 等）的提交。

---

## 2026-09-09 回归检查（未提交）

对定制分支增量功能执行回归审计（报告：
`docs/regression-review-2026-09.md`），修复 A/B 两级问题并补齐测试。

### A 级（500 / 数据不一致 / 安全边界）

- 结构化导航递归环保护（自引用/互相引用的 `.sidebar.json` 不再导致
  RecursionError，警告并回退普通索引）
- Git 仓库切换后对推送目标写入 `receive.denyCurrentBranch=updateInstead`
  （Git HTTP 服务器启动时已写入；导入替换后缺失时补写）
- 文档导入与 Git 同步两条「替换仓库」流程的互斥收紧：登记维护日志先于
  上传，Git 同步的 `blocked()` 在整段上传期间可见；登记后复查活动 Git 任务，
  冲突即撤销登记并返回 409
- 远程仓库地址校验收紧：拒绝 `ext::`/`file::` 等传输前缀、私网/回环地址、
  Windows 盘符路径与含 `..` 的 scp 路径
- 导入中断恢复补分支：目标仓库路径不存在且无备份时解除维护并提示
- 扫描任务上传改为 1 MiB 分块流式读取 + `LimitedReader` 上限，
  不再整份读入内存

### B 级（功能缺陷，均已带回归测试）

- 点号路径保留（`v1.2/guide` 不再变成 `v12/guide`）；缓存键加入工作树
  指纹（未提交的修改立即可见）
- `index.html` 等主动内容附件强制 `as_attachment` 下载（同源存储型 XSS）
- 导航编辑器：乐观并发校验（过期 revision 拒绝保存）、原子写、写锁
- 标题编号修复：`###` 先于 `##` 不再产生 "0.1"；跨级跳跃不再产生 "1.1.0.1"
- 侧栏分类按标签配置推导（自定义标签不再错位高亮）
- `storage.list` 深度剪枝；`_add_custom_entries` 改为单趟建索引
- 导入进度写入容错、数据库不可用不再静默降级、超时文案与实际一致
- 旧仓库备份清理在所有生产路径可达，不再无限堆积
- 接口管理：外部请求异常兜底（HTTPException/RecursionError/畸形 IPv6）、
  30x 不自动跟随（urllib 会把 POST 降级为 GET）、路径段编码 `.`、
  `_text`/`_count` 语义修正、`packageSourceType` 白名单校验并转发、
  变更类端点统一白名单归一化 + `Cache-Control: no-store`、资产关系
  `targetAssetCode` 不再回退到源资产、关联属性列展示为可读文本
- Git 同步：线程启动失败释放锁、工作线程捕获 `BaseException`、
  轮询接口触发崩溃恢复、前端轮询 30 分钟上限
- 二次提示重建 toast 关闭按钮并重置计时器（`interface-feedback.js`）

### C 级（无用代码）

- 删除零引用文件与死代码（见报告第 3 节列表，含两个纯转发函数）

### D 级（测试）

- 浏览器测试统一 `--no-sandbox` 参数，受限环境不再因 Chromium 沙箱
  启动失败；无浏览器时明确 skip，`OTTERWIKI_REQUIRE_BROWSER_TESTS=1`
  时直接失败，避免静默零覆盖

### 已知限制（本次不修复）

- 单副本、单 Web 进程假设；`PROXY_HEADER` 与多空间不兼容；
  匿名读取被门禁覆盖（均为有意设计，说明已同步到
  `settings.cfg.skeleton`、`help_admin.md`、`docs/multi-space-upgrade.md`）

---

## 2026-09-08

### Added
- 对齐 IDP 接口管理页面（907d5d4）
- 重构设置中心 UI 并统一样式规范：新增共享样式表 `settings.css` 与页头片段 `settings_page_header`，侧边栏改为分组导航，全部管理子页统一卡片与表单风格（216829c）

### Changed
- 隐藏旧版文档导入入口（f5e6eb5）

### Fixed
- 修复 Git 导入相关问题（991ffbd）
- 修复数据库迁移问题（ea4c0d3）

## 2026-09-07

### Added
- 新增导出项目配置（ec33a46）
- 新增数据库迁移启动配置（fdc1262）
- 新增启动健康检查配置（390d459）

## 2026-09-05

### Added
- 支持按空间配置 Git 仓库与异步同步（3fbaecb）
- 支持文档异步导入及进度轮询（d06f32b）

### Fixed
- 修复主界面侧栏与主题切换样式问题（2c5bc7f）

## 2026-09-04

### Added
- 新增多空间与用户组权限体系及全局文档字号配置（9a27460）
- 重构登录与找回密码页面样式（ef3457e）

### Fixed
- 主界面样式更新；构建前主动从内网仓库拉取基础镜像（fb3c77c）

## 2026-09-03

### Added
- 配置 CI 流水线（d5192a0）

## 2026-09-02

### Added
- 完善接口管理快照、资产关系与 API 网关页面（fb5565d）
- 完善交易资产、审计日志与版本对比功能（5e92928）
- 工作台统计卡片支持点击跳转对应模块（8b3ce43）

### Changed
- 优化交易资产目录的关键字搜索框（536bae1）
- 接口管理提示统一改为右上角悬浮 toast（ec18d37）

### Fixed
- 修复接口管理表格表头吸顶异常（9eea2e8）
- API 网关列表禁用列宽拖拽并默认等分（f24dbe1）
- 应用管理子 tab 列表禁用列宽拖拽（50b38b8）
- 加宽扫描任务操作列避免按钮截断（5118c06）
- 加宽文档侧栏最小宽度避免导航 tab 折行（0b8b09e）
- 收紧非管理员导航菜单权限（4cd47b8）

## 2026-09-01

### Added
- 新增管理员接口管理工作台（65c6419）
- 接口管理新增系统、应用与扫描任务管理（da5a7ad）
- 表格列宽支持本地记忆并固定操作列（a246d3c）

### Fixed
- 扫描任务分页「页」改为文字单位并支持回车跳页（4842a8a）

## 2026-08-31

### Added
- 优化编辑器加载并新增可视化文档导航管理（0d583ed）

## 2026-08-28

### Added
- 新增管理端重置仓库并导入文档功能（33d65ae）
- 新增侧边目录宽度拖拉功能（32348e5）
- 新增对表格的解析（ff01925）

### Changed
- 优化 mac 与 windows 运行环境差异（90bd306）
- 优化 windows 显示 icon 错误问题（0861628）

### Fixed
- 修复 windows 导入后无法删除备份的问题（af9ada4、efa7a53）

## 2026-08-27

### Added
- 支持 APStack6 规则驱动的完整文档导航（18f71fe）
- 添加 Windows 迁移文档（4c41efa）
- 数据库迁移脚本（cb134a4）

### Changed
- 界面全面汉化（314ac57）

### Performance
- 优化结构化文档导航与页面加载性能（021ca02）

---

## 功能模块总览

| 模块 | 主要工作 |
|---|---|
| **接口管理** | 管理员工作台、系统/应用/扫描任务管理、快照、交易资产、资产关系、API 网关、审计日志、版本对比、工作台跳转、toast 提示 |
| **文档与编辑** | APStack6 规则驱动导航、可视化导航管理、表格解析、侧栏拖拉、编辑器加载优化、全局字号 |
| **导入/导出与同步** | 按空间配置 Git 仓库异步同步、异步导入+进度轮询、管理端重置仓库导入、导出项目配置、旧版入口隐藏 |
| **权限与多空间** | 多空间与用户组权限、非管理员菜单权限收紧 |
| **UI/UX** | 全面汉化、登录/找回密码页重构、设置中心统一 UI、主题切换与侧栏样式修复 |
| **工程化与部署** | 迁移脚本与启动迁移配置、健康检查、CI 流水线、内网镜像拉取、mac/windows 环境适配 |
